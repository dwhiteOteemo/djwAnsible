---
- hosts: localhost
  vars:
    description: “Test VSphere Connectivity”
  tasks:
  - name: get vm facts
    vmware_guest_facts:
      hostname: vcentera.atpiad1.local
      username: atpiad1\disapiuser
      password: '@p1r3@d0n1Y4D1$'
      datacenter: DCA 
      validate_certs: no
      name: dis01tst01
    delegate_to: localhost
    register: facts
